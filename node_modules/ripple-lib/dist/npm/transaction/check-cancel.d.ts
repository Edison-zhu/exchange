import { Instructions, Prepare } from './types';
export declare type CheckCancelParameters = {
    checkID: string;
};
declare function prepareCheckCancel(address: string, checkCancel: CheckCancelParameters, instructions?: Instructions): Promise<Prepare>;
export default prepareCheckCancel;
