import { Instructions, Prepare } from './types';
import { Amount } from '../common/types/objects';
export declare type CheckCashParameters = {
    checkID: string;
    amount?: Amount;
    deliverMin?: Amount;
};
declare function prepareCheckCash(address: string, checkCash: CheckCashParameters, instructions?: Instructions): Promise<Prepare>;
export default prepareCheckCash;
